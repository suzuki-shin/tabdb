(function() {
  var edgeCase, ifChecks, ifNullChecks, undefinedChecks, unlessChecks, voidChecks, whileAndFor;

  ifChecks = function() {
    if (x) true;
    if (!x) return true;
  };

  ifNullChecks = function() {
    if (typeof x === "undefined" || x === null) true;
    if (x === null) return nah;
  };

  voidChecks = function() {
    if (typeof x === "undefined" || x === null) true;
    if (x === void 0) nah;
    if (typeof x === "undefined" || x === null) return true;
  };

  undefinedChecks = function() {
    if (typeof x === "undefined") return nah;
  };

  edgeCase = function() {
    if (!x === y) return nah;
  };

  unlessChecks = function() {
    if (typeof x !== "undefined" && x !== null) true;
    if (x !== null) nah;
    if (typeof x !== "undefined") return wat;
  };

  whileAndFor = function() {
    var _results;
    while (typeof x === "undefined" || x === null) {
      true;
    }
    while (x === null) {
      true;
    }
    a;
    _results = [];
    while (!(typeof x !== "undefined" && x !== null)) {
      true;
      _results.push(2);
    }
    return _results;
  };

}).call(this);
